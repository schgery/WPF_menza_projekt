<Window x:Class="WPF_menza_projekt.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_menza_projekt"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" DisplayMemberPath="nev" ItemsSource="{Binding vendegek}" x:Name="vendegeklv">
            
        </ListView>

        <StackPanel Grid.Column="4">
            <Label HorizontalAlignment="Center">Új vendég</Label>
            <StackPanel Orientation="Horizontal">
                <Label Content="Név:"/>
                <TextBox x:Name="hozzaadastb" Width="100"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Diák:"/>
                <CheckBox x:Name="hozzaadascb" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Content="Hozzáadás" x:Name="hozzaadasbt" Click="hozzaadasbt_Click" />

            <Label HorizontalAlignment="Center">Új leves</Label>
            <StackPanel Orientation="Horizontal">
                <Label Content="Név:"/>
                <TextBox x:Name="levestb" Width="100"/>
            </StackPanel>
            <Button Content="Hozzáadás" x:Name="levesbt" Click="levesbt_Click" />

            <Label HorizontalAlignment="Center">Új főétel</Label>
            <StackPanel Orientation="Horizontal">
                <Label Content="Név:"/>
                <TextBox x:Name="foeteltb" Width="100"/>
            </StackPanel>
            <Button Content="Hozzáadás" x:Name="foetelbt" Click="foetelbt_Click" />

            <Label HorizontalAlignment="Center">Új desszert</Label>
            <StackPanel Orientation="Horizontal">
                <Label Content="Név:"/>
                <TextBox x:Name="desszerttb" Width="100"/>
            </StackPanel>
            <Button Content="Hozzáadás" x:Name="desszertbt" Click="desszertbt_Click" />
        </StackPanel>

        <StackPanel Grid.Column="3">
            <Label HorizontalAlignment="Center">leves</Label>
            <ComboBox DisplayMemberPath="nev" ItemsSource="{Binding levesek}" x:Name="levescb"/>
            <Label HorizontalAlignment="Center">foetel</Label>
            <ComboBox DisplayMemberPath="nev" ItemsSource="{Binding foetelek}" x:Name="foetelcb"/>
            <Label HorizontalAlignment="Center">deszsert</Label>
            <ComboBox DisplayMemberPath="nev" ItemsSource="{Binding desszertek}" x:Name="desszertcb"/>
            <DatePicker x:Name="datumpicker"/>
            <Button Content="Hozzáadás" x:Name="napietkezesbt" Click="napietkezesbt_Click"/>
        </StackPanel>
        <ListView Grid.Column="1" ItemsSource="{Binding napietkezesek}" x:Name="napietkeslv">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding datum, StringFormat='{}{0:yyyy.MM.dd}'}" FontWeight="Bold"/>
                        <TextBlock>
                            <Run Text="leves:"/>
                            <Run Text="{Binding leves.nev}"/>
                            <LineBreak/>
                            <Run Text="foetel:"/>
                            <Run Text="{Binding foetel.nev}"/>
                            <LineBreak/>
                            <Run Text="desszert:"/>
                            <Run Text="{Binding desszert.nev}"/>
                        </TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
